# Core
pandas>=2.1
python-dotenv>=1.0

# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# UI
streamlit>=1.32

# LLM providers (all optional at runtime — only the selected one is imported)
google-genai>=1.0
groq>=0.11
httpx>=0.27

# Tests
pytest>=8.0
